#
# Engine configuration loaded and used by IntensiGame(Base) class.
#

# High-level engine configuration:
GameTiming.maxFramesPerSecond = 32
GameTiming.ticksPerSecond = 32

# Buffer text strings? (Trade mem for perf? Loses transparency!)
BitmapFontGenerator.buffered = false
BitmapFontGenerator.maxBufferWidth = 240
BitmapFontGenerator.maxBufferHeight = 72

# Force screen size to these values:
DirectScreen.width = 480
DirectScreen.height = 800
